int minJumps(int arr[], int n) {
    int jumps = 0, steps = 0, maxrange = 0;
    if (n <= 1)
        return 0;
    if (arr[0] == 0)
        return -1;

    maxrange = steps = arr[0];
    jumps = 1;

    for (int i = 1; i < n - 1; i++) {
        if (i == n - 1)
            return jumps;

    }
}